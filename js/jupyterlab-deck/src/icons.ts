import { LabIcon } from '@jupyterlab/ui-components';

import DECK_SVG from '../style/img/deck.svg';

export namespace ICONS {
  export const deck = new LabIcon({ name: 'deck:deck', svgstr: DECK_SVG });
}
